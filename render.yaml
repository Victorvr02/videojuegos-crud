services:
  - type: web
    name: videojuegos-crud
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: videojuegosdb
          attribute: connectionString